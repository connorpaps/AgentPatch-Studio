interface:
  display_name: "Postgres"
  short_description: "Connect to Postgres, run SQL/diagnostics, inspect schemas, and review queries."
  brand_color: "#0064a5"
  icon_small: "./assets/postgresql-small.svg"
  icon_large: "./assets/postgresql.png"
  default_prompt: "Connect to the target Postgres database or inspect local schema code, guide schema/data-type/index decisions, review SQL/query performance, use common PostGIS or pgvector patterns when relevant, and summarize the result."
